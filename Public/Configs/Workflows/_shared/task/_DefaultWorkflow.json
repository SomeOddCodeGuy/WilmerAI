{
  "RespondingModel": "Worker-Endpoint",
  "RespondingPreset": "Worker-Endpoint",
  "RespondingResponseTokenSize": 8000,
  "MinMessagesToSendRespondingLLM": 10,
  "MaxTokensToSendRespondingLLM": 15000,
  "nodes": [
    {
      "displayOnlyComment": "Meant for a workhorse model. Generally small and light, if available",
      "title": "Run Task Workflow",
      "type": "CustomWorkflow",
      "workflowName": "Task",
      "workflowUserFolderOverride": "_common",
      "scoped_variables": [
        "{RespondingModel}",
        "{RespondingPreset}",
        "{RespondingResponseTokenSize}",
        "{MinMessagesToSendRespondingLLM}",
        "{MaxTokensToSendRespondingLLM}"
      ]
    }
  ]
}
